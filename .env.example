# ── Redis ──────────────────────────────────────────
REDIS_URL=

# ── MySQL (config storage only) ────────────────────
MYSQL_URL=

# ── Admin ──────────────────────────────────────────
ADMIN_SECRET=your-very-secret-admin-key-here

# ── Default rate limit (used when client has no config) ──
DEFAULT_MAX_TOKENS=
DEFAULT_REFILL_RATE=
DEFAULT_WINDOW_SIZE=
DEFAULT_ALGORITHM=

# ── App ────────────────────────────────────────────
APP_ENV=
APP_PORT=
CONFIG_CACHE_TTL=